apiVersion: apps/v1
kind: Deployment
metadata:
  name: devume-client
  namespace: devume
spec:
  selector:
    matchLabels:
      app: devume-client
  replicas: 2
  template:
    metadata:
      labels:
        app: devume-client
    spec:
      containers:
        - name: devume-client
          image: 10.105.58.187:5000/devume-client:9
          imagePullPolicy: Always
          envFrom:
          - secretRef:
              name: devume-secret
          ports:
          - containerPort: 8081
          readinessProbe:
            httpGet:
              path: /api/health
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 5

